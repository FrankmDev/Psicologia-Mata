---
const values = [
  { title: 'Confidencialidad', description: 'Todo lo que compartes permanece en un espacio seguro y privado.' },
  { title: 'Compromiso', description: 'Acompañamiento constante en cada paso de tu camino terapéutico.' },
  { title: 'Rigor', description: 'Técnicas basadas en evidencia científica y formación continua.' },
  { title: 'Empatía', description: 'Escucha activa y comprensión profunda de tu situación.' },
];
---

<section class="section bg-charcoal text-cream relative overflow-hidden" id="valores">
  
  <!-- Fondo -->
  <div class="absolute inset-0 opacity-[0.02]" style="background-image: radial-gradient(circle at 1px 1px, white 1px, transparent 0); background-size: 24px 24px;"></div>
  <div class="absolute top-0 right-0 w-1/2 h-1/2 bg-sage/5 blur-[100px]"></div>
  
  <div class="container-editorial relative z-10">
    <div class="grid lg:grid-cols-2 gap-10 lg:gap-16 items-start">
      
      <!-- Izquierda -->
      <div data-reveal>
        <div class="mb-3">
          <span class="label-section text-sage-light">Mi compromiso</span>
        </div>
        <h2 class="title-section text-cream mb-4">
          Principios que guían <em class="text-sage-light">cada sesión</em>
        </h2>
        <p class="text-body text-cream/60 mb-8 max-w-md">
          Un entorno seguro donde construir la confianza necesaria para el trabajo terapéutico.
        </p>
        
        <a href="/contact" class="btn btn-outline border-cream/20 text-cream hover:bg-cream hover:text-charcoal text-xs">
          Conoce mi enfoque
        </a>
      </div>
      
      <!-- Valores -->
      <div class="grid sm:grid-cols-2 gap-4">
        {values.map((value, index) => (
          <div 
            class="group p-5 rounded-lg border border-cream/10 hover:border-sage/30 hover:bg-cream/5 transition-all"
            data-reveal
            data-reveal-delay={100 + index * 80}
          >
            <div class="w-10 h-10 rounded-lg bg-cream/5 border border-cream/10 flex items-center justify-center mb-4 group-hover:bg-sage group-hover:border-sage transition-colors">
              <span class="font-serif text-lg text-sage-light group-hover:text-cream">0{index + 1}</span>
            </div>
            <h3 class="font-serif text-lg text-cream mb-1">{value.title}</h3>
            <p class="text-body text-xs text-cream/50">{value.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
