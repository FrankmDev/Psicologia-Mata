---
const steps = [
  {
    number: '01',
    title: 'Primera Sesión',
    subtitle: 'Conocernos',
    description: 'Cuéntame qué te trae aquí. No necesitas tener las palabras perfectas — solo la honestidad de empezar.',
  },
  {
    number: '02',
    title: 'Evaluación',
    subtitle: 'Entender',
    description: 'Juntos entendemos de dónde viene lo que sientes. No para señalar el pasado, sino para dibujar un camino diferente.',
  },
  {
    number: '03',
    title: 'Intervención',
    subtitle: 'Acompañar',
    description: 'Te doy herramientas que funcionan, para que puedas manejar lo que antes te desbordaba. Y practicamos hasta que te sientas seguro.',
  },
  {
    number: '04',
    title: 'Seguimiento',
    subtitle: 'Consolidar',
    description: 'Cuando estés listo, espaciamos las sesiones. Mi objetivo es que un día no me necesites — y que sepas que aquí estaré si me necesitas de nuevo.',
  },
];

const highlights = [
  { value: '50 min', label: 'Duración por sesión' },
  { value: 'Presencial', label: 'Consulta en Córdoba' },
  { value: 'Personalizado', label: 'Adaptado a ti' },
];
---

<section class="section bg-charcoal text-alabaster relative overflow-hidden" id="proceso">

  <!-- Fondo sutil -->
  <div class="absolute -top-1/2 -right-1/4 w-[80vw] h-[80vw] rounded-full bg-ink blur-[120px] opacity-50 pointer-events-none"></div>

  <div class="container-editorial relative z-10">

    <!-- Header estilo Services -->
    <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-8 lg:gap-16 mb-16 lg:mb-24">
      <div data-reveal class="max-w-2xl">
        <div class="mb-8">
          <span class="label-section !text-terracotta-light">El Proceso</span>
        </div>
        <h2 class="title-section !text-alabaster">
          Cuatro pasos hacia tu <em class="!text-terracotta-light">bienestar</em>
        </h2>
      </div>

      <div class="flex flex-col items-start lg:items-end gap-6" data-reveal data-reveal-delay="100">
        <p class="text-body-sm !text-taupe-light max-w-sm lg:text-right">
          No hay dos personas iguales, ni dos procesos iguales. Pero todos empiezan con una conversación.
        </p>
      </div>
    </div>

    <!-- Timeline compacto estilo services.astro -->
    <div class="border-t border-border-strong/30">
      {steps.map((step, index) => (
        <div
          class="group flex flex-col md:flex-row items-start md:items-center py-8 md:py-10 px-4 -mx-4 md:px-6 md:-mx-6 lg:px-8 lg:-mx-8 border-b border-border-strong/30 hover:bg-ink/20 transition-colors duration-500 rounded-lg"
          data-reveal
          data-reveal-delay={index * 100}
        >
          {/* Columna número */}
          <div class="w-full md:w-1/5 mb-3 md:mb-0 md:pr-8 flex items-center gap-4">
            <span class="text-label text-terracotta-light w-8">{step.number}</span>
            <span class="flex-1 h-[1px] bg-terracotta-light/30 group-hover:bg-terracotta-light/60 transition-colors duration-500 origin-left group-hover:scale-x-100 hidden md:block"></span>
          </div>

          {/* Columna título */}
          <div class="w-full md:w-1/3 mb-3 md:mb-0 md:pr-8">
            <p class="text-label-xs !text-taupe-light uppercase tracking-[0.15em] mb-1">{step.subtitle}</p>
            <h3 class="font-serif text-xl md:text-2xl text-alabaster group-hover:text-terracotta-light transition-colors duration-500">
              {step.title}
            </h3>
          </div>

          {/* Columna descripción */}
          <div class="w-full md:w-5/12">
            <p class="text-body-sm !text-taupe-light/80 leading-relaxed">
              {step.description}
            </p>
          </div>
        </div>
      ))}
    </div>

    {/* Stats destacados - estilo minimalista */}
    <div class="mt-16 lg:mt-20" data-reveal data-reveal-delay="200">
      <div class="grid grid-cols-3 gap-8 lg:gap-0">
        {highlights.map((item, index) => (
          <div class={`text-center ${index < 2 ? 'lg:border-r lg:border-border-strong/20' : ''}`}>
            <span class="font-serif text-2xl lg:text-3xl text-terracotta-light block mb-1">{item.value}</span>
            <span class="text-label-xs !text-taupe-light/70">{item.label}</span>
          </div>
        ))}
      </div>
    </div>

    {/* CTA final - estilo unificado */}
    <div class="mt-16 pt-12 border-t border-border-strong/30" data-reveal data-reveal-delay="300">
      <div class="flex flex-col md:flex-row items-center justify-between gap-8">
        <div class="text-center md:text-left">
          <p class="font-serif text-xl text-alabaster mb-1">¿Te gustaría que habláramos?</p>
          <p class="text-body-sm !text-taupe-light/70">La primera consulta es simplemente una conversación. Sin compromiso, sin presión.</p>
        </div>
        <div class="flex flex-col sm:flex-row gap-4">
          <a href="/contact" class="btn btn-primary group">
            <span>Pedir cita</span>
            <svg class="w-4 h-4 transition-transform duration-300 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17.25 8.75L21 12m0 0l-3.75 3.25M21 12H3" />
            </svg>
          </a>
          <a href="tel:+34661471971" class="btn !bg-transparent !border-border-strong/30 !text-alabaster hover:!bg-alabaster/10">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 002.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 01-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 00-1.091-.852H4.5A2.25 2.25 0 002.25 4.5v2.25z" />
            </svg>
            <span>Llamar</span>
          </a>
        </div>
      </div>
    </div>

  </div>
</section>
